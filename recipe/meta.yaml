{% set name = "contourpy" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/contourpy-{{ version }}.tar.gz
  sha256: 37a040e73a5849124c04e193d39c2b930e9b544292dea40b9d6e3bfaa706450d

build:
  number: 0
  skip: true  # [py<37]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - pip
    - pybind11 >=2.6
    - python
  run:
    - numpy >=1.16
    - python

test:
  imports:
    - contourpy
    - contourpy.util
  commands:
    - pip check
    - python -c "import contourpy as c; c.contour_generator(None, None, [[0, 1], [2, 3]]).lines(0.9)"
  requires:
    - pip

about:
  home: https://github.com/contourpy/contourpy
  summary: Calculating contours of 2D quadrilateral grids from Python
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - ianthomas23
